# Frontend/backend dependency folders — regenerated by npm ci
node_modules/

# Terraform local state and cache
.terraform/
terraform.tfstate
terraform.tfstate.backup
*.tfvars

# EC2 private key files — never commit these
*.pem

# Terraform plan/apply log output — never commit these
plan-output.txt
apply-output.txt