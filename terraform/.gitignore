# Terraform local state and cache — see reasoning below
.terraform/
terraform.tfstate
terraform.tfstate.backup
*.tfvars

# Frontend/backend dependency folders — regenerated by npm ci
node_modules/

# EC2 private key files — never commit these
*.pem

# Log terraform plan success and failures plan-output — never commit these
plan-output.txt

# Log terraform apply success and failures in apply-output — never commit these
apply-output.txt